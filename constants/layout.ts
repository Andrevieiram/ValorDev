/** Altura da área de ícone + rótulo na tab bar (sem safe area inferior) */
export const TAB_BAR_INNER_HEIGHT = 48;

/** Espaço superior interno da tab bar */
export const TAB_BAR_PADDING_TOP = 8;

/** Folga entre o fim do scroll e a tab bar nas telas com abas */
export const SCREEN_CONTENT_GAP_ABOVE_TAB_BAR = 16;
